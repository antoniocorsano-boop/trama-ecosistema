name: PR92 Alignment Workflow Retired

# Temporary migration helper retired after ECO-02/P1 closure alignment.
# Kept inert pending removal from the pull request branch.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary PR92 alignment workflow retired; no repository mutation."
